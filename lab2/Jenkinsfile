/* Requires the Docker Pipeline plugin */
pipeline {
    agent any
    stages {
        stage('build') {
            steps {
		sh 'pwd'
                sh 'pip install -r ../requirements.txt'
            }
        }
        stage('test') {
            steps {
                sh 'echo test'
            }
        }
    }
}
