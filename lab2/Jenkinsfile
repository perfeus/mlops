/* Requires the Docker Pipeline plugin */
pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                sh 'python3'
            }
        }
        stage('test') {
            steps {
                sh 'python'
            }
        }
    }
}
